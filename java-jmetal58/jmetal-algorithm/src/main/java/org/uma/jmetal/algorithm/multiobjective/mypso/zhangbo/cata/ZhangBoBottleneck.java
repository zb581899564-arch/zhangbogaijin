package org.uma.jmetal.algorithm.multiobjective.mypso.zhangbo.cata;

/** Stable bottleneck order is also the deterministic tie-break order. */
public enum ZhangBoBottleneck {
  SEQ,
  MAC,
  WOR,
  SET,
  FAT,
  BAL
}
