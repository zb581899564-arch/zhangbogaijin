package org.uma.jmetal.algorithm.multiobjective.mypso.p8;

/** The five official P8 ablation matrices plus an explicitly isolated diagnostic line. */
public enum P8MatrixKind {
  FV, FM, QP, VNS, FULL, CONTROL, DIAGNOSTIC
}
