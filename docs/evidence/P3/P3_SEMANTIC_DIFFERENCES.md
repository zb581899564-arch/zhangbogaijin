# P3 三语义与作者实际差异

| 维度 | `PUBLISHED_STOCHASTIC` | `DETERMINISTIC_CANONICAL` | `AUTHOR_ACTUAL` |
|---|---|---|---|
| 用途 | 论文随机分支的可重放实现 | 后续改进算法默认生产解码 | 仅诊断作者旧源码 |
| 工人首轮 | seed派生的无放回排列 | 工人编号升序 | 活动路径默认worker数组为0 |
| 破平 | ETC/FIFO后稳定工件编号，FAM/工人取较小编号 | 同左，但不调用随机源 | 多处行为由旧实现隐式决定 |
| 设置时间 | `SUT/WE` | `SUT/WE` | 活动路径使用`ST+10%×ST` |
| 第一阶段MA/WA | 直接使用四向量 | 直接使用四向量 | 活动计算没有直接落实两向量 |
| 目标 | `[Cmax,TEC,TWC]` | `[Cmax,TEC,TWC]` | 七槽位；TWC写入索引6 |
| 右移 | 有 | 有 | 活动`evaluate()`路径未见论文右移 |
| 随机性 | seed=`20260808`可重放 | 无随机调用 | 默认Solution含未注入seed的`Random`/`shuffle` |

作者实际诊断使用未修改的`EDHHFSPW(20,2,3,1)`与现有`EADHFSP/20_2_3_1.txt`，通过隔离适配器成功调用；编码未被修改。七槽位观察值为：

```text
[331.4505494505495, 13984.724228152796, 0.0, 2.0, 2.0, 1.0, 10929.527472527472]
```

该观察只证明作者旧路径的当前实际行为可被捕获，不把它接入`DhhfspProblem`，也不把其数值当作论文黄金预言。

