# P9训练机小规模先导验收

## 范围

- 远端主机：`inspur-NP5570M5`，用户：`inspur`。
- 独立目录：`/home/inspur/aicomp/zhangbo-java-p9-pilot-20260810`。
- 运行时间：`2026-08-10T11:11:24+08:00` 至 `2026-08-10T11:12:09+08:00`。
- Java：OpenJDK `11.0.27`；产物仍为Java 8兼容字节码。
- CPU边界：`taskset -c 0-3`；最大Java堆：`4 GiB`；未使用GPU。
- 未恢复、终止或修改训练机上既有的暂停任务。

## 实验设计

| 配置 | 本次回答的问题 |
|---|---|
| `B0` | 规范HMOPSO-QGS、统一SUT和显式MA/WA能否运行 |
| `B1` | 完整疲劳累积、自然恢复、工时反馈和疲劳选工能否接入 |
| `B5` | CFVF、容量6谱系档案、Q-pbest/Q-gbest及分块冻结能否接入 |
| `FULL` | O10–O13和含FAT上下文的CA-TA-VNS能否接入完整链路 |

- 实例：第四章10工件桥实例、`20_2_3_1`。
- 种子：`20260808/20260809/20260810`。
- 种群：`100`。
- 单运行上限：`20000 FE`，为论文配置`500000 FE`的4%。
- 运行数：`4×2×3=24`。

## 硬验收结果

- 远端退出码：`0`。
- 完成记录：`24/24`，全部为`COMPLETED`。
- 实际FE范围：`19941–20000`；总完整评价：`479886`。
- 非法解：`0`。
- CFVF异常repair：`0`。
- 同实例、同seed的四个配置初始四向量哈希一致，无初始种群漂移。
- 24个最终非支配前沿文件全部存在，三个目标均为有限值。
- `FULL`六次运行均产生非零局部完整评价，完整链路未落入空CA-TA路径。

`P8RunRecord.caTaEvaluations`在当前实现中累计局部搜索完整评价，包含固定O1–O9路径，不能单独作为“CA-TA已启用”的唯一证据。CA-TA启用状态还必须结合`FULL`机制向量、配置哈希和既有Test/Apply事件测试解释。

## 初步趋势，仅作诊断

在相同的疲劳评价语义下，`B1→B5`在两个实例上均改善了平均最小`Cmax`和`TEC`；`B5→FULL`在第四章桥实例的三个平均最小目标均改善，在`20_2_3_1`上改善`TEC/TWC`但`Cmax`变差。多目标权衡和3个种子的样本量都不足以支持优越性或显著性结论。

`B0`不含疲劳，目标定义的实际工时语义与`B1/B5/FULL`不同，不能把`B0→B1`的数值上升解释为算法退化；该配对首先用于验证疲劳模型接入。

## 状态边界

本次只证明规范编解码和三个创新点可以在训练机上的同一Java/jMetal 5.8生产链中完成小规模运行。它不是完整34项消融，不包含20–30次独立运行、500000 FE、参数敏感性或显著性检验，因此：

```text
sampled_reproduction_accepted=false
full_reproduction_accepted=false
```

关键原始证据见`run-records.csv`、`pilot-summary.csv`、`fronts/`、`console.log`、`preflight.txt`和`exit-code.txt`。
