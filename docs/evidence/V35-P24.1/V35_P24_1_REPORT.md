# V35-P24.1 冻结修订

本修订不改变生产算法机制，只关闭两个冻结证据歧义：

- D-070 中 `A3=184.40 / +0.059` 明确为预评价重复计费修复前历史值；当前证据统一为 `A3=198.32 / +0.049`。
- 保存 JDK 17 完整回归命令，固定包含 `--add-opens=java.base/java.lang=ALL-UNNAMED`。
- `source-sha256.csv` 已从修订后的当前源码树重建，包含更新后的 ROADMAP 哈希。
- 本测试在同一次验收中连续写盘两次，并要求目录内全部冻结物字节级一致。

P25A 仅在本修订、205 项回归和六模块打包通过后启动。
