# 01 Offline cardinality audit

本阶段实现了精确去重、K25/K30/K50固定基数子集和近重复率计算。当前目录不包含新训练结果。
主指标仍读取完整`decision-front`；K30仅用于论文展示，K25/K50仅用于敏感性。

未来若执行既有结果离线审计，必须保存输入front的SHA-256、去重前后点数、三种K值输出和输入
顺序置换一致性结果，且不得改写原front。

