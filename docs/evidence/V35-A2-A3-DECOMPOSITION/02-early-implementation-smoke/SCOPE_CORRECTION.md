# 早期 D0 运行范围更正

`seed-20260822-D0_A2_CONTROL-50k` 在专用 Runner 的 `--preflight` 固定 2,000 FE
入口加入前完成。该 CLI 当时只允许 50,000 FE，因此此目录实际是一次完整的 D0
实现烟测，而不是 V35-A3-D 的四臂 2,000 FE 贯通门。

该运行保留原始 `configuration.txt`、状态、前沿、控制器日志和 SHA-256 清单，未被
删除或覆盖；但它不计入后续 12 条预登记 D0--D3 诊断样本，也不计入任何论文统计。
四臂固定 2,000 FE 门将在 `03-preflight/` 重新独立执行。
