# P8迁移前证据隔离说明

本目录在P1–P8概念复核前生成的运行、指标和参考前沿统一标记为：

```text
legacy_pre_ablation_switches
```

保留这些文件用于追溯，不删除、不覆盖。它们包含48条已完成运行，但当时仍有18个正式标签未暴露、第四章黄金实例未接入作者兼容生产路径，并且`B7`与`FULL`使用相同配置，因此不能作为校正后P8的当前消融证据。

校正后的注册表、桥接清单、运行记录和报告写入相邻的`P8-v2`目录。
