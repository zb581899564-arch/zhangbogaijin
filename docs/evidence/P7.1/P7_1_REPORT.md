# P7.1 O1-O13独立邻域实施报告

日期：2026-08-09  
状态：`completed`

## 结论

O1–O13已作为独立、可计数的邻域组件实现并通过黄金实例和真实小实例审计。它们没有接入`ZhangBoMOHPSOQ.perturbation()`；CA-TA上下文、Test-and-Apply和生产主循环接入仍属于P7.2。

## 组件

- 请求/结果/诊断/候选上限均为独立契约；所有完整评价经过计数网关。
- O1–O3同步移动JS及FA/MA/WA资源包；O4–O6只改WA；O7–O9只改MA。
- O10建立工件、机器、工人DAG并从零时差关键块稳定生成最多6个候选。
- O11按固定SUT、疲劳增量和实际时长归一化等权预测前三工人；无SUT审计路径关闭SET并采用0.5/0.5。
- O12按四子群固定权重归一化预测AT/EC/WC/疲劳增量，同时修改MA/WA，最多评价3个组合。
- O13支持FMAX和FE焦点，仅生成JS/WA候选；未增加目标工人自然恢复的候选被拒绝，不引入休息基因。

## 评价预算与确定性

- O1–O9单次上限各1；O10=6；O11=3；O12=3；O13=4。
- 论文10工件黄金实例及`20_2_3_1`分别运行四子群O1–O13矩阵，自动断言每实例不超过104次完整评价。
- 实际审计计数为：论文10工件黄金实例76 FE，`20_2_3_1`为92 FE，均未超过104 FE上限。
- 固定seed `20260808`连续100次结果与诊断一致；异seed产生可定位JS候选差异。
- 不适用邻域返回明确原因；预测和非法检查0 FE。O13若已评价候选但最终无恢复增益，已花费FE仍如实记录。

## 验证范围

- 定向测试覆盖资源包工件身份不变、输入对象不变、关键DAG、SUT来源、四子群权重、MA/WA联合修改、FMAX/FE入口、候选上限和方向选择。
- P5.1定向测试、P6.4 2000 FE门槛及Java 8构建通过。
- P2–P7.1定向回归共102项，0 failures、0 errors；六模块打包成功。旧核心回归保持651项、0 failures、3个P1既有errors、6 skipped，错误签名未变化。
- 未实现CA-TA状态机、等预算Test-and-Apply、主循环局部搜索接入、主动休息、多技能或额外目标。

## 状态标志

```text
o10_o13_engineering_validated=true
o10_o13_scheme_aligned=true
sampled_reproduction_accepted=false
full_reproduction_accepted=false
```
