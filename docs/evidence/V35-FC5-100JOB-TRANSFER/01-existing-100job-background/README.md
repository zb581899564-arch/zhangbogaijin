# 已有 100-job 背景证据

本目录只存放不改变历史原始运行的背景汇总与可用性说明。

- A0↔A2 的 `100_8_3_1` 已触发 100-job 否决门：相对 A0，A2 的中位 `ΔHV=-22.3210%`、`ΔIGD=-40.2505%`。
- A2↔A4 的 `100_5_3_1` 已触发 100-job 否决门：相对 A2，A4 的中位 `ΔHV=-12.96%`、`ΔIGD=-76.31%`。
- FC-5 已在 `20_2_3_1` 观察到 archive 保存的好 Cmax 解与 working population 脱节；该结果是本次迁移假设的来源，不能直接外推为 100-job 根因。
- 现有确认运行保存了 Cmax 生命周期和部分教师信息，但没有每轮完整 merge pool 的严格 ND 基数、四方向代表、PDDR拒绝原因与“代表→有效后代”链，因此不能满足 H1 四项判定。后续仅在这些缺字段上作行为中性的遥测重放。
