# 已取代的中间运行

- 初次B2P/B3串行对照未在两次运行前重置作者jMetal全局随机单例，第二次运行继承前一次随机状态，导致CFVF事件伪差异。
- 根因确认后，固定显式初始种群的消融测试在每次运行前重置相同`JMetalRandom` seed；最终B2P/B3的结果、Qg、CFVF、PDDR和FE一致。
- 该校正只用于工程对照，没有修改普通Runner，也没有声称作者遗留初始化可完整重放。

