# V35 正式实验工作区

本目录只保存 `V35 vs HMOPSO-QGS-F` 正式实验的协议、冻结输入、原始前沿和审计证据。

当前状态：`BLOCKED`。这里的模板和预检输出不代表已开始正式矩阵、已冻结参考前沿或已获得统计结论。

目录约定：

- `00_protocol/`：批准门、冻结清单、运行登记和预检报告；
- `03_main_45x20/raw-fronts/`：仅在 FC-9 与相应正式矩阵批准后存放单臂原始前沿；
- `08_reference_and_statistics/`：只能在所有已批准原始前沿齐备、参与算法集合冻结后创建每实例 `PFref`；当前只保留禁止说明。

基线名称固定为 **HMOPSO-QGS-F 公平适配基线**。它不是“李明哲原算法”，也不得把 `A0_AUTHOR_DIAGNOSTIC`、旧 Shift-on 或旧压力语义结果写入本目录的正式母表。

